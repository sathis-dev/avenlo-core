[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.dashboard"
watchPatterns = ["services/dashboard/**", "packages/shared/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
